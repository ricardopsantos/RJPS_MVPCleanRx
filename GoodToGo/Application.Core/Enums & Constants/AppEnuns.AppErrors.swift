//
//  GoodToGo
//
//  Created by Ricardo P Santos on 2019.
//  Copyright © 2019 Ricardo P Santos. All rights reserved.
//

import Foundation
/*
extension AppEnuns {
    private init() {}

    enum AppErrors {
        case noInternet
        case notImplemented
        case invalidURL
        case unknownError

        func toFoundationError() -> Error {
            func errorWith(code:AppEnuns.AppCodes) -> Error { return NSError(domain:code.localizableString, code:code.rawValue, userInfo:nil) }
            switch self {
            case .noInternet    : return errorWith(code: .notImplemented)
            case .notImplemented: return errorWith(code: .notImplemented)
            case .invalidURL    : return errorWith(code: .invalidURL)
            case .unknownError  : return errorWith(code: .invalidURL)
            }
        }
    }
    
      
}
*/
